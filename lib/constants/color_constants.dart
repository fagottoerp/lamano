import 'package:flutter/material.dart';

class ColorConstants {
  // Palette WhatsApp-style
  static const themeColor     = Color(0xFF25D366);
  static const primaryColor   = Color(0xFF128C7E);
  static const accentGreen    = Color(0xFF25D366);

  // Backgrounds
  static const bgApp          = Color(0xFFF0F2F5);
  static const bgChat         = Color(0xFFEFEAE2);
  static const bgSent         = Color(0xFFD9FDD3);
  static const bgReceived     = Color(0xFFFFFFFF);

  // Cards & surfaces
  static const cardWhite      = Color(0xFFFFFFFF);
  static const surfaceLight   = Color(0xFFF0F2F5);

  // Text
  static const textPrimary    = Color(0xFF111B21);
  static const textSecondary  = Color(0xFF667781);

  // Borders & dividers
  static const divider        = Color(0xFFE9EDEF);
  static const greyColor      = Color(0xFF667781);
  static const greyColor2     = Color(0xFFE9EDEF);

  // Compat aliases
  static const bgDark         = bgApp;
  static const cardDark       = cardWhite;
  static const surfaceDark    = surfaceLight;

  // Alert / status colors
  static const policeBlue     = Color(0xFF3B82F6);
  static const dangerRed      = Color(0xFFEF4444);
  static const trafficOrange  = Color(0xFFF59E0B);
  static const motoboyGreen   = Color(0xFF25D366);
  static const accidentYellow = Color(0xFFF59E0B);
}
